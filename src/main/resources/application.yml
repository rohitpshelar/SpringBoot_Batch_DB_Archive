spring:
  datasource:
    main:
#      load script from org/springframework/batch/core/schema-mysql.sql
      jdbc-url: jdbc:mysql://localhost:3306/main_db
      url: jdbc:mysql://localhost:3306/main_db
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    archive:
      jdbc-url: jdbc:mysql://localhost:3306/archive_db
      url: jdbc:mysql://localhost:3306/archive_db
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
  batch:
    jdbc:
      initialize-schema: ALWAYS
    job:
      enabled: false

  sql:
    init:
      mode: always

logging:
  level:
    root: INFO
